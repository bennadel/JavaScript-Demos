import { Observable } from 'rxjs/Observable';
export interface Hook<T> {
    apply(input$: Observable<T>): Observable<T>;
}
export declare const identity: Hook<any>;
export declare function composeHooks<T>(hooks: Hook<T>[]): (input$: Observable<T>) => Observable<T>;
